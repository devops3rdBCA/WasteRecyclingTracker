name: SonarCloud Analysis

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud-placeholder:
    name: SonarCloud Analysis (Placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder - SonarCloud analysis skipped
        run: |
          echo "✅ SonarCloud analysis skipped - no secrets configured"
          echo "To enable SonarCloud analysis, configure the following secrets:"
          echo "  - SONAR_TOKEN"
          echo "  - SONAR_ORGANIZATION"
          echo "See .github/SECRETS.md for more information"
