name: SonarCloud Analysis

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud-backend:
    name: SonarCloud Backend Analysis
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for better relevancy

      - name: Check SonarCloud configuration
        id: check-sonar
        run: |
          if [ -z "${{ secrets.SONAR_TOKEN }}" ] || [ -z "${{ secrets.SONAR_ORGANIZATION }}" ]; then
            echo "⚠️ SonarCloud not configured - skipping analysis"
            echo "To enable SonarCloud, configure SONAR_TOKEN and SONAR_ORGANIZATION secrets"
            echo "configured=false" >> $GITHUB_OUTPUT
          else
            echo "✅ SonarCloud configured - proceeding with analysis"
            echo "configured=true" >> $GITHUB_OUTPUT
          fi

      - name: Set up JDK 17
        if: steps.check-sonar.outputs.configured == 'true'
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Cache SonarCloud packages
        if: steps.check-sonar.outputs.configured == 'true'
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Build and analyze backend
        if: steps.check-sonar.outputs.configured == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          cd backend
          mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
            -Dsonar.projectKey=${{ secrets.SONAR_ORGANIZATION }}_waste-recycling-tracker-backend \
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }} \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml

  sonarcloud-frontend:
    name: SonarCloud Frontend Analysis
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check SonarCloud configuration
        id: check-sonar
        run: |
          if [ -z "${{ secrets.SONAR_TOKEN }}" ] || [ -z "${{ secrets.SONAR_ORGANIZATION }}" ]; then
            echo "⚠️ SonarCloud not configured - skipping analysis"
            echo "To enable SonarCloud, configure SONAR_TOKEN and SONAR_ORGANIZATION secrets"
            echo "configured=false" >> $GITHUB_OUTPUT
          else
            echo "✅ SonarCloud configured - proceeding with analysis"
            echo "configured=true" >> $GITHUB_OUTPUT
          fi

      - name: Set up Node.js
        if: steps.check-sonar.outputs.configured == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        if: steps.check-sonar.outputs.configured == 'true'
        run: |
          cd frontend
          npm ci

      - name: Run tests with coverage
        if: steps.check-sonar.outputs.configured == 'true'
        run: |
          cd frontend
          npm run test:coverage || echo "No tests configured yet"

      - name: SonarCloud Scan
        if: steps.check-sonar.outputs.configured == 'true'
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          projectBaseDir: frontend
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_ORGANIZATION }}_waste-recycling-tracker-frontend
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.sources=src
            -Dsonar.exclusions=**/*.test.js,**/*.test.jsx,**/node_modules/**
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info
