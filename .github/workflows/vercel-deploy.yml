name: Deploy to Vercel
on:
  push:
    branches:
      - main
    paths:
      - frontend/**
      - vercel.json
      - .github/workflows/vercel-deploy.yml
  pull_request:
    branches:
      - main
    paths:
      - frontend/**
      - vercel.json
      - .github/workflows/vercel-deploy.yml

jobs:
  vercel-placeholder:
    name: Vercel Deployment (Placeholder)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Placeholder - Vercel deployment skipped
        run: |
          echo "✅ Vercel deployment skipped - no secrets configured"
          echo "To enable Vercel deployment, configure the following secrets:"
          echo "  - VERCEL_TOKEN"
          echo "  - VERCEL_ORG_ID"
          echo "  - VERCEL_PROJECT_ID"
          echo "See .github/SECRETS.md for more information"
